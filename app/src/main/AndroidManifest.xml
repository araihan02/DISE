<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ac.id.pei.rpl.dise">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DISE">
        <activity android:name=".AddActivity"></activity>
        <activity android:name=".Splash2Activity" />
        <activity android:name=".LaporanActivity" />
        <activity android:name=".MakananActivity" />
        <activity android:name=".ExtremeActivity" />
        <activity android:name=".OpsiMakananActivity" />
        <activity android:name=".OlahragaActivity" />
        <activity android:name=".BmiActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>